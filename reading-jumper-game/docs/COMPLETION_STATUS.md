# 代码完成与外部阻塞

## 已完成

- 独立 H5 工程与独立微信 `web-view` 小程序壳。
- 五场景玩法、每场五题、逐场结算、切场景清分、250 条联调题记录、生命/关卡/连击 HUD 和分原因结算。
- 稳定题库初始化、音频真机解锁与失败降级、独立会话和独立埋点缓存。
- 小程序批量消息、分享配置、题库和埋点接口透传。
- 服务端 Range、可信代理限流、埋点轮转和仅上传 `release/web` 的发布保护。
- 类型、依赖分层、循环依赖、复杂度、单测、资源预算和双视口 E2E 门禁。
- 桌面端与移动端均覆盖开场、答题和结算截图；冻结基线和原设计静态区域像素门禁均已建立。
- 五个主题均有独立正式切图路径、主题结算背景和独立布局；残余视觉差异见 `docs/PIXEL_ALIGNMENT_AUDIT.md`。
- 阅读角色 idle/action 状态切换已接入，正式连续动画可直接替换对应资源。
- 正式题库门禁检查内容审批状态、重复率和分年级覆盖。
- 背景响应式取景已修正，五个主题已使用独立题板、选项、角色和 HUD 布局。
- 结算页已按主题背景预留区域重建动态分数、星级、排行榜和答题回顾。
- 已建立固定桌面/移动基线与遮罩区域像素门禁；门禁自检和真实单像素变更均能返回失败。

## 2026-07-12 验证结果

- `npm run check`：39 个单元测试、6 个服务端测试全部通过。
- `npm audit --omit=dev`：0 个生产依赖漏洞。
- Web 构建：6.64 MB；资源预算：4.22 MB 磁盘、29.52 MB 解码 RGBA。
- 桌面、移动和星际/美食/诗词逐主题 E2E 全部通过。
- 候选发布包：`release/20260712231000`，manifest 共 184 个文件。
- 正式小程序发布门禁按预期拒绝占位 AppID 和 `example.com` 域名。
- 正式题库门禁按预期拒绝当前占位题库：50/250 个唯一内容模板，且未分年级终审。

## 外部阻塞

- 素材包未提供非马里奥主题逐帧角色动画和授权字体文件。
- 经客户终审并标记 `contentStatus: "approved"` 的分年级正式题库。
- 微信后台把已上传开发版设置为体验版。
- 微信开发者工具真机编译、iOS/Android 多机型及长时间性能验收。

## 2026-07-13 场景结算改造验证

- 正常成功路径已改为同一场景连续完成五题，第五题后结算；前四场点击“进入下一场景”，第五场进入原有最终结算。
- 切换场景时积分、场内答题记录、题目计时和连击统计清零，倒计时重启；生命按客户当前要求保留。
- 生命耗尽、超时、空题库和运行错误仍是终局，不会进入下一场景。
- 当前场景顺序由配置决定：马里奥、深海、星际、美食、诗词；顺序尚待客户最终确认，调整配置即可更换。
- 桌面 1440×810 与移动 844×390 均跑完 25 题，验证前四场中间结算、切场景分数归零和第五场最终结算。
- 本轮验证：47 项单元测试、6 项服务端测试、类型/架构/题库/资源/Web 预算、生产构建及生产依赖审计全部通过。

## 2026-07-13 客户马里奥素材接入

- 马里奥开场/结算角色、6 帧待机、22 帧向上跳、BGM 及开始/答题/反馈/转场/结算音效已替换为客户素材。
- 28 张原始大图未直接导入；按实际显示尺寸生成两张边缘扩展图集，角色动画解码约 5.89 MB，避免约 112 MB 的原始帧显存占用。
- 客户 95.4 秒 BGM 从 2.18 MB 压缩为 0.55 MB；只在马里奥主题启用，进入其他主题时停止并切回默认 BGM。
- 最新构建为 7.92 MB，资源解码为 35.42 MB；桌面/移动 25 题 E2E 和 10 场景性能门禁通过。
- 详细映射、未使用音效及原因见 `docs/CUSTOMER_ASSETS.md`。

## 2026-07-13 性能复核（以本节为准）

- 已撤回“程序实现层面已闭环”的表述。当前只能确认本地自动化覆盖的代码路径通过。
- 已关闭构建产物中的 2 秒 Cocos 启动屏，并用构建门禁检查 `totalTime: 0`。
- 五个关卡主题已拆为五个独立 Asset Bundle；运行时保留当前/预取主题，并在引用释放后卸载旧 Bundle。
- 占位 BGM 从 176,444 字节 WAV 改为 32,852 字节 MP3；五个短反馈音改为零网络 WebAudio 合成音。
- 最新本机独占 headless Chrome 10 场景周期结果：启动到 ready 890.0 ms，p95 17.0 ms，p99 22.4 ms，最长任务 159 ms，DrawCall P95/峰值 27/27，三角形 P95/峰值 318/318。
- 强制 GC 后总堆增长 498,684 字节，后半程增长 3,220 字节，单周期最大增长 427,380 字节；三项均通过门禁，但不能据此外推 30 分钟稳定。
- 本地 DrawCall 和三角形已纳入自动门禁；尚未测量真机 FPS、峰值原生内存、DrawCall、发热、弱网和连续 30 分钟稳定性，不能宣称“极致性能”或生产性能验收完成。

## 2026-07-15 完整主题切图与像素审计

- 五套答题主题和五套结算背景已接入；星际、美食、诗词不再是占位主题。
- Web 发布 `20260715223117` 已上传 COS，公开链路完成 25 题验证；阅读小程序开发版 `1.0.3` 已上传。
- 本轮 Web 发布 `20260715181408` 已上传 COS 并按 index/题库 SHA-256、题库数量和 MoveNet
  模型公开校验；阅读小程序开发版 `1.0.4` 已上传。
- 微信后台尚未确认把 `1.0.4` 选为体验版；CI 预览二维码已生成，但不能写成“体验版已完成”。
- 原包没有主题连续动画；只有基础版继续使用现有待机/跳跃图集，其他主题角色仍是静态切图。
- 新增像素差分审计表明答题页差异像素为 54.58%-73.97%，结算页为 86.37%-95.87%，当前不能称为像素级对齐。
- 主要根因是背景取景、共用固定布局、额外 HUD/摄像头、结算组件未按切图重建和字体缺失；完整证据见 `docs/PIXEL_ALIGNMENT_AUDIT.md`。

## 2026-07-16 视觉实现与门禁复核

- 完整五主题 25 题桌面/移动 E2E 通过；未发现结算按钮位置回归或页面运行时异常。
- 结算页已修正总分与背景奖台标签的相对位置、星级与操作按钮重叠、浅色主题答题回顾对比度和末行越界。
- `npm run check:visual` 通过 8 个固定基线场景：开场、五主题答题、桌面结算和移动结算。
- `npm run check:visual:self-test` 通过；实际篡改单个未遮罩像素时，门禁输出 `FAIL` 并返回退出码 `1`。
- 资源预算复核通过：93 个资源、7.08 MB 磁盘、72.47 MB 总解码 RGBA，当前/预取主题最大常驻 39.27 MB。
- Performance Trace 已生成于 `test-results/performance/runtime-trace.json`：p95 17.0 ms、p99 20.5 ms、
  最长任务 127 ms、DrawCall 峰值 29、三角形峰值 231、三轮堆增长 438,160 bytes。
- 当前“通过”代表相对冻结基线没有视觉回归，不代表相对原始 2560×1440 示意图已经零像素差异；字体、动态内容和产品新增 HUD 仍是独立验收项。

## 2026-07-16 正式开场、摄像头与最终性能复核

- 开场已替换为正式“阅读跳跳乐”标题和引导词，并接入金币桶、管道前后层及正式角色。
- 开场预展示摄像头空框，点击开始后才请求权限；桌面和横屏移动端尺寸、16:9 比例、
  边界及 ready 状态标题栏隐藏均有 E2E 断言。
- 马里奥继续使用 6/22 帧图集；其他四主题只有程序化呼吸/升降/跳跃，不能写成原画逐帧动画完成。
- 冻结基线 8/8、单像素破坏自检、原设计静态区域 10/10 均通过。开场原稿差异像素
  仍为 10.94%，结算为 5.83%-11.67%，不能称零像素一致。
- 最新构建 250 个文件、16.91 MB，距离 17 MB 门禁仅约 0.09 MB，后续新增资源前必须继续压缩。
- 最新 Performance Trace：ready 946.7 ms、p95 18.2 ms、p99 18.9 ms、最长任务 115 ms、
  DrawCall P95/峰值 17/29、三角形 P95/峰值 67/231、三轮堆增长 431,564 bytes。
- 自动化技术链路已通过，但 `contentStatus` 仍为 `placeholder`，且没有真机 30 分钟、
  发热、耗电和峰值原生内存报告。

## 2026-07-16 发布状态

- 最终 Web release：`release/20260716063941`，已上传 302 个 Web 文件到 COS
  `reading-jumper/` 前缀。
- 公开地址：`https://game.xyouxing.com/reading-jumper/index.html`。
- `index.html` SHA-256：
  `3f68970b52e5c135fd6f2572c0111da8cbffade32cd1ec3609904bac3cbb9260`。
- `question-bank.json` SHA-256：
  `33938dd70f5c880de519cba3edec2c0c14e1d3ddf103336baf394f4867801828`。
- 公开校验确认 250 题、MoveNet 模型有效、浏览器标题为“阅读跳跳乐”，线上开场
  `gameView=intro`、摄像头 `poseState=idle` 且框体为 `328x188`。
- 微信 CI 已上传开发版 `1.0.6`。CI 上传不等于体验版设置，仍需在微信后台选择该版本为体验版。

## 2026-07-16 性能与移动视觉继续收口

- 85 张 PNG 已执行无损重压缩，移除未使用的多线程 WASM 变体；Web 构建从
  250 个文件、16.91 MB 降为 249 个文件、16.29 MB，距 17 MB 门禁约 0.71 MB。
  仍未达到 15.5 MB 理想目标，不继续以删除普通 WASM 回退换取体积数字。
- TF.js WASM 脚本改为 WebGL 初始化失败后才加载；SIMD 与普通 WASM 回退均保留。
  本机真实 WASM MoveNet 测试首次就绪 166 ms、热推理 p95 31 ms。
- 移动冻结视觉基线由 1 个结算页扩展到开场、五主题答题和最终结算共 7 个场景；
  桌面与移动共 14/14 通过，单像素破坏自检仍会失败。
- 新增 `npm run benchmark:runtime:soak`：按墙钟运行 30 分钟，采集帧、Long Task、
  DrawCall、三角形、运行时错误、逐周期 GC 堆趋势和前后台恢复，并独立输出报告。
- 5 分钟本机桌面 soak 完成 34 周期和 3 次恢复，错误 0，p95 17.4 ms、p99 24.5 ms；
  堆总增长 2,081,784 bytes，距离 2 MB 门槛仅 15,368 bytes，后半程增长
  305,540 bytes。可判断当前没有明显线性泄漏，但不能据此宣称 30 分钟真机验收完成。
- 新 Web release `20260716051927` 已上传 301 个文件、18,651,520 bytes 到 COS
  `reading-jumper/` 前缀。公开 index SHA-256 为
  `16e3578e7f79548c44fc136f9340450dd2c659e6bbc2bb26dd6a5c52e44a4ef9`，
  题库 SHA-256 仍为
  `33938dd70f5c880de519cba3edec2c0c14e1d3ddf103336baf394f4867801828`。
- 微信 CI 开发版 `1.0.7` 已上传，描述为“性能长测与移动视觉门禁完善”；仍需在微信
  后台把该开发版选为体验版，CI 无法证明该后台动作已经完成。

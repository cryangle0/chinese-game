/** Top-right secondary CTA on stage settlement (design 1440×810, origin center). */
export const SETTLEMENT_HOME_CHIP = Object.freeze({
  x: 390,
  y: 342,
  width: 180,
  scale: 0.86,
  label: '返回首页',
});
